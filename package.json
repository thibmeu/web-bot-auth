{
  "name": "web-bot-auth-repo",
  "description": "The monorepo for web-bot-auth",
  "type": "module",
  "keywords": [
    "web-bot-auth",
    "typescript"
  ],
  "author": "Thibault Meunier",
  "private": true,
  "scripts": {
    "build": "npm run build -w http-message-sig -w jsonwebkey-thumbprint -w web-bot-auth",
    "format": "prettier --write . && oxlint examples packages --ignore-pattern **/dist/** --fix",
    "lint": "prettier --check . && oxlint examples packages --ignore-pattern **/dist/**",
    "test": "npm run typecheck && npm run test -w http-message-sig -- --run && npm run test -w jsonwebkey-thumbprint -- --run && npm run test -w web-bot-auth -- --run && npm run test -w verification-workers -- --run",
    "typecheck": "npm run build --silent && npm run typecheck -w http-message-sig -w web-bot-auth"
  },
  "license": "Apache-2.0",
  "version": "0.0.0",
  "devDependencies": {
    "oxlint": "1.76.0",
    "prettier": "3.9.6",
    "rimraf": "6.1.3",
    "tsdown": "0.22.14",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "packageManager": "npm@11.3.0"
}
